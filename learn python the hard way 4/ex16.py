#coding:utf-8from sys import argvscript, filename = argvprint "We're going to earse %r." % filenameprint "If you don't want that, hit CTRL-C (^C)."print "If you want that, hit RETURN."raw_input("?")print "Opening the file..."target = open(filename, 'w')print "Thuncating the file. Goodbye!"target.truncate()print "Now I'm going to ask you for three lines."line1 = raw_input("line1: ")line2 = raw_input("line2: ")line3 = raw_input("line3: ")print "Now I'm going to write these to the file."target.write(line1)target.write("\n")target.write(line2)target.write("\n")target.write(line3)target.write("\n")print "And finally, we close it"target.close()'''E:\case-python\learn python the hard way 4>python ex16.py test.txtWe're going to earse 'test.txt'.If you don't want that, hit CTRL-C (^C).If you want that, hit RETURN.?ll1Opening the file...Thuncating the file. Goodbye!Now I'm going to ask you for three lines.line1: ll1line2: ll2line3: ll3Now I'm going to write these to the file.And finally, we close it'''